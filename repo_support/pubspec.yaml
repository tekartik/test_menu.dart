name: test_menu_repo_support
description: Mustache repo (tools)
version: 0.2.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  dev_test: '>=0.13.0'
  path:
  pub_semver:
  lints:
  process_run: '>=0.10.0'

