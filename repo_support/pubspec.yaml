name: test_menu_repo_support
description: Mustache repo (tools)
version: 0.2.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dev_dependencies:
  dev_build: '>=0.13.0'
  path:
  pub_semver:
  lints: '>=2.0.0'
  process_run: '>=0.10.0'

